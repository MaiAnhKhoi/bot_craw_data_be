# =====================================================================
# bot_craw_data — thư viện CHỈ dùng khi phát triển / kiểm thử.
# Image production cài `requirements.txt` thôi, không cài file này.
# =====================================================================
-r requirements.txt

pytest>=8.0            # bộ test; marker `live` (chạm Google Maps thật) bị CI loại bằng -m "not live"
pytest-asyncio>=0.24   # engine cào là async (playwright.async_api) — thiếu gói này thì mọi
                       # `async def test_*` bị pytest BỎ QUA kèm cảnh báo, test "xanh" mà không chạy
ruff>=0.6              # lint + sắp xếp import; cấu hình ở `ruff.toml`
openpyxl>=3.1          # ĐỌC LẠI file .xlsx vừa xuất trong test (tests/test_export_pipeline.py).
                       # Runtime chỉ GHI nên dùng xlsxwriter; openpyxl không lên image thật
